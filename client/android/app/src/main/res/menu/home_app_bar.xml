<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/home_bar_search"
        android:icon="@drawable/ic_search"
        android:title="@string/home_search"
        app:showAsAction="ifRoom">
    </item>

    <item
        android:id="@+id/home_bar_category"
        android:icon="@drawable/ic_hamburger"
        android:title="@string/home_category"
        app:showAsAction="ifRoom">
    </item>

    <item
        android:id="@+id/home_bar_alarm"
        android:icon="@drawable/ic_alarm"
        android:title="@string/home_alarm"
        app:showAsAction="ifRoom">
    </item>

</menu>